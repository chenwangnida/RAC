leftVmMem * ((vmCpuOverhead - (((coCpu + leftVmCpu) * (coMem + vmMemOverhead)) * (coMem - (coCpu + leftVmCpu)))) * (((coCpu + leftVmCpu) * (coMem + vmMemOverhead)) * ((coCpu + leftVmCpu) * (coMem - (coCpu + leftVmCpu)))))
