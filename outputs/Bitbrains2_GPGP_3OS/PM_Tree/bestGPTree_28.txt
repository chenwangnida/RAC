((((PmUsedMem - pmMem) / vmMem) / vmMem) / vmMem) + (((PmCPU + ((PmUsedMem - pmMem) / vmMem)) / (((vmMem / vmMem) / vmMem) / vmMem)) / (((PmUsedMem - pmMem) / vmMem) + (((vmMem * PmUSedCPU) + (PmUsedMem + pmMem)) + (PmUsedMem + pmMem))))
