(((((PmUsedMem - pmMem) * (PmUsedMem - pmMem)) * ((PmUsedMem - pmMem) * (PmUsedMem - pmMem))) * (((PmUsedMem - pmMem) * (PmUsedMem - pmMem)) * ((PmUSedCPU - PmCPU) * (PmUsedMem - pmMem)))) * ((((PmUsedMem - pmMem) * (PmUSedCPU - PmCPU)) * ((PmUsedMem - pmMem) * (PmUsedMem - pmMem))) * (((PmCPU * vmMem) + VmCPU) + VmCPU))) * ((((PmUsedMem - pmMem) * ((PmUsedMem - pmMem) * (PmUsedMem - pmMem))) * (((PmUsedMem - pmMem) * (PmUsedMem - pmMem)) * ((PmUsedMem - pmMem) * (PmUsedMem - pmMem)))) * ((((PmUsedMem - pmMem) * (PmUsedMem - pmMem)) * ((PmUsedMem - pmMem) * (PmUSedCPU - PmCPU))) * (((PmUsedMem - pmMem) * (PmUSedCPU - PmCPU)) * ((PmUsedMem - pmMem) * (PmUsedMem - pmMem)))))
