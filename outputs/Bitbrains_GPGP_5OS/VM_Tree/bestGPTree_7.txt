vmMemOverhead - ((((vmMemOverhead - coCpu) * (coCpu * leftVmCpu)) * ((leftVmCpu - leftVmMem) * (coCpu * coMem))) + (((leftVmCpu * (vmMemOverhead - leftVmMem)) * ((leftVmCpu - leftVmMem) * (coCpu * coMem))) + ((coCpu * (coCpu * leftVmCpu)) + (vmCpuOverhead / (coCpu * leftVmCpu)))))
