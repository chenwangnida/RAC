((((vmMem + vmMem) / vmMem) / vmMem) - ((((PmUsedMem - pmMem) / vmMem) * (PmUsedMem - pmMem)) * (((PmUsedMem - pmMem) / vmMem) / vmMem))) * (((((PmUsedMem - pmMem) / vmMem) / vmMem) / vmMem) - ((((vmMem - VmCPU) * PmCPU) * vmMem) * vmMem))
