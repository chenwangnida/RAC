(((((vmMem * PmUSedCPU) / pmMem) * ((PmUsedMem - pmMem) * (PmUsedMem - pmMem))) * PmUsedMem) * (((PmUsedMem - pmMem) * ((PmUsedMem - pmMem) * (PmUsedMem - pmMem))) * (((PmUsedMem - pmMem) * (PmUsedMem - pmMem)) * ((PmUsedMem - pmMem) * (PmUsedMem - pmMem))))) * ((((PmUsedMem - pmMem) * ((PmUsedMem - pmMem) * (PmUsedMem - pmMem))) * (((NoOfCore - PmUSedCPU) * (PmUsedMem - pmMem)) * ((PmUsedMem - pmMem) * (PmUsedMem - pmMem)))) * (((PmUsedMem - pmMem) * ((PmUsedMem - pmMem) * (PmUsedMem - pmMem))) * (((PmUsedMem - pmMem) * (PmUsedMem - pmMem)) * ((NoOfCore - PmUSedCPU) * (PmUsedMem - pmMem)))))
