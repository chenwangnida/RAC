coMem - ((((leftVmCpu + coCpu) * (leftVmCpu + coCpu)) * (coCpu * (leftVmCpu + coCpu))) * ((leftVmCpu + coCpu) * (((leftVmMem * coMem) + (vmMemOverhead / coCpu)) * ((leftVmCpu + coCpu) * (leftVmMem * coMem)))))
